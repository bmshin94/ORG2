# Market Package final delivery — tracking (2026-09-17)

Follow-up to ORG2 #1899 / Cloud #104 / #105 (all merged 2026-09-17). Each row
is closed only by evidence recorded under `evidence/` in this directory or by a
commit in this branch. "Blocked" rows name the external dependency.

| # | Item | Kind | Status |
|---|------|------|--------|
| 1 | Billing tail: source of the extra small request after external Claude Code calls | analysis + doc | **Closed**: it is Claude Code's automatic title call (`ai-title` record). It is billed to the Package pinned in `ANTHROPIC_DEFAULT_HAIKU_MODEL` (the connection's default), not to the Package picked with `/model`. Reproduced with a `--settings` overlay on the default `~/.claude` home: foreground → Coding for beginner 47127 µUSD, title → Sonnet overlap 2263 µUSD, both settled at Admin 45%/35%. See `evidence/claude-code-title-call.md`. |
| 2 | Auxiliary calls (title/memory/compaction) attribution | code + doc | **Closed** in this branch as explicit behaviour: they bill the connection's default Package (the pinned `ANTHROPIC_DEFAULT_*_MODEL` aliases); only the chat follows `/model`. Per-session routing is not possible because Claude Code sends auxiliary calls under a different `metadata` session identity, so the proxy cannot correlate them. App Connections (Claude Code CLI, package picker) now says so (`harnessConnections.marketApps.auxiliaryBilling`, 15 locales). |
| 3 | "Configuration changed outside ORG2" after external `/model` | code | **Closed** — superseded by row 10: Claude Code is now launched with an ORG2-owned overlay (`claude --settings <overlay>`), so nothing Claude Code writes to `~/.claude/settings.json` can conflict. The interim runtime-drift tolerance (`model`, `theme`, `effortLevel`) stays for manifests that still point at the native file until they are migrated. |
| 4 | Claude Desktop isolation for acceptance | tooling + acceptance | Launch recipe **verified 2026-09-17**: `"/Applications/Claude.app/Contents/MacOS/Claude" --user-data-dir=<dir>` (binary, not `open`) starts an independent instance that writes only under `<dir>` (own `claude_desktop_config.json`, caches, storage) while the main app keeps running. `open -n -a Claude --env CLAUDE_USER_DATA_DIR=…` does **not** isolate: the second process opened the main data directory. Still open: point ORG2 (`ORGII_EXTERNAL_HISTORY_HOME`) at the same home, apply the Desktop connection, confirm the isolated instance reads the `Claude-3p` profile, then dual-Package selection, call, billing, restart and restore. |
| 5 | Codex full chain (ORG2 + official app, Luna/model switch, context, billing) | acceptance + Market fix | Root cause found (`evidence/luna-reserve-mechanics.md`): Luna Reserve is the model slug `gpt-reserve`; Cloud PR #117 sends it as the fallback. **2026-09-17 23Z** (`evidence/codex-package-chain-20260917.md`): ORG2 → Codex connection applies (after the PATH finding in row 13); Codex CLI 0.154.0 reaches the Market through the Package; the fallback did not fire because #117 only covered the workspace route — ported to the Package route (`75e69e6`, `016055d`, 8 tests). Now blocked on the CPA adapter: CLIProxyAPI answers `400 model_not_found` to `gpt-reserve` (router-for-me/CLIProxyAPI#5568). Ordinary chain re-check after the weekly reset (2026-09-19 08:23Z / 09:22Z). |
| 6 | Login / authorization lifecycle (no saved grant, signed-out ORG2 → web enable → launch → auto sign-in → Package sync; account / Cloud URL switch; expired grant) | acceptance | Open — needs the user at the browser login step. |
| 7 | Credential renewal and supplier rotation | acceptance | Renewal: open. Rotation: **Blocked** — only one healthy Claude supplier. |
| 8 | Admin access restriction (Neonforge, Harry, Junyu Feishu open_ids) | config + acceptance | **Blocked** — same-app open_ids not provided. |
| 9 | Final-build regression: successful failed-message Retry, external CLI restart + config restore, foreground/hidden/closed resource usage | acceptance | **Retry passed** (`evidence/failed-message-retry-f006d33b6.md`): injected 502, Resend on the tail failed message reran the turn, reply `PACKAGE-RETRY-GATE-OK`, one foreground request + title settled, failed history kept. Restart passed (`evidence/final-build-restart-84330574d.md`); Disconnect/re-apply verified. Resource samples (RSS): background window 135–147 MB / 0–1.4 % CPU, foreground 112–145 MB / 0.4–0.8 %, closed window see row text below. **Bug found (row 12)**: after Resend the open view shows neither the working footer nor the reply until the session is reopened. |
| 10 | Claude Code overlay: never modify the user's `~/.claude/settings.json` (user decision 2026-09-17) | code | **Landed** in this branch: overlay target `~/.orgii/cli-config-profiles/claude_code/overlay/settings.json` regenerated on every apply; Open terminal runs `claude --settings <overlay>` in the user's normal Claude home (history/`--resume` intact); apply refuses while the user's file has `apiKeyHelper`, `env.ANTHROPIC_API_KEY`, `env.CLAUDE_CODE_OAUTH_TOKEN` or a cloud-provider flag; legacy native manifests are restored from backup at startup (non-forcing); UI shows the overlay path and "Disconnect" instead of "Restore original setup". Codex and Claude Desktop unchanged. Applies to Direct (API key / Claude profile) connections too: a plain `claude` no longer sees them — use Open terminal or `claude --settings <path>`. Still to verify on the private build: apply → open terminal → call → disconnect. |
| 11 | Session-provenance hooks are written into the user's real `~/.claude/settings.json` at every launch (`crates/agent-cli/src/session_provenance/mod.rs` resolves `home_dir()/.claude/settings.json`, ignoring `ORGII_EXTERNAL_HISTORY_HOME`) | code (follow-up) | Open, diagnosis confirmed by an adversarial review (2026-09-18): `config_path()` resolves all 11 agent hook targets through `app_paths::home_dir()`, which reads no env override, so every instance — including the isolated acceptance one — instruments the REAL user home. The fix must NOT be a bare external-home override: it has to reuse the precedence already shipped in `generic_config.rs` (external-home, then the agent's own `CLAUDE_CONFIG_DIR`/`CODEX_HOME`, then the default), or the two resolvers disagree about where an agent's config lives; and the tests must take the crate env lock. Tier 2 (are hooks in the user's real settings.json acceptable at all, given row 10) still needs the user's decision — moving them into the overlay would lose exactly the external sessions they exist to observe. |
| 12 | After Resend-retry of a failed tail message the open conversation view shows no "Agent working" footer and no reply until the session is reopened (reply exists in transcript/DB within 6 s) | code | **Fixed** (`0820dc670`) and reviewed (`b8da4d270`): a Resend on a `runlanded-` tail row goes through the canonical failed-intent router, not the edit/rewind branch. The review proved the pre-fix path was worse than recorded — it truncated the ROOT by timestamp with `revertFiles: true`, i.e. reverted the root session's working tree for an edit on a child's row — and that the new tests fail against the pre-fix hook. Two follow-ups from it: the resend now carries the row's turn identity so the retried copy can be collapsed (was keeping the failed turn and its error as model context forever), and editing an OLDER child row appends instead of rewinding, which is deliberate until a child-scoped rewind exists (the row carries no did-it-fail flag). GUI re-verification still pending. |
| 13 | Finder-launched app cannot start an npm-installed Codex: the shell PATH probe falls back to the login-only PATH, `codex` (`#!/usr/bin/env node`) fails with `env: node: No such file or directory`, the app only says "Cannot verify the installed harness version" | code | **Fixed** (`7e9803765`). Measured cause: the two login-shell PATH probes ran in turn, so the login-only fallback started only after the interactive probe had spent the whole 5 s budget; an interactive zsh costs ~3.1 s from a GUI process here (nvm's auto-use spawns node) vs ~0.45 s from a terminal, and lost that race at bootstrap every time. Now both run concurrently under the same budget, only the last non-empty line of stdout is read (a chatty rc file used to splice a banner onto the first directory), and each probe's outcome is replayed into the log once tracing is up. Follow-up: resolution still hands back a shebang shim whose interpreter is absent, and a spawned CLI inherits the same PATH for its own children. |
| 14 | Luna Reserve fallback for Packages (Cloud PR #117) | Market fix | Market side **done** (Cloud #117 `75e69e6`, `016055d`, `f282a65`; CI green). The reserve is now **proven** to be a separate quota bucket: at 2026-09-18 00:21Z, with `codex:primary` at 100 % and every Luna call 429ing, a `gpt-reserve` call returned 200 answering as `gpt-5.6-luna`. Still blocked at the adapter, and the cheap ways in are now measured out: `oauth-model-alias` cannot introduce a model the registry lacks (tried both directions), and the registry URL is a compiled-in constant. What remains: patch/fork CLIProxyAPI and pin our own image; or route only the reserve hop through the CPA management `api-call` path (the only path that has ever delivered a reserve answer, but it puts the adapter's admin credential on a buyer data path and streaming is unverified); or per-auth `model_aliases` in the OAuth auth JSON (documented, untested, writes a seller credential file); or wait for router-for-me/CLIProxyAPI#5568. **Needs the user's decision.** |

No ORG2 installer, Beta, release or tag is produced by this work.
